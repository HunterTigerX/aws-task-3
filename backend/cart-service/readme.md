`npm run deploy ` will run the sctipt to deploy the database and the app and fill the DB using lambda function and SQL file.  
`npm run fill` will run the sctipt to fill the DB using typescript file and SQL file.
Database user is `postgres`
Database password is `postgres`
Database url is `cartdb.czwk442a8alq.eu-central-1.rds.amazonaws.com`
Database port is `5432`
Database name is `cartdb`